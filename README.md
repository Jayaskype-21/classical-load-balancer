# classical-load-balancer
# It is old version load balancer
# It is works at both transport layer and application layer
# It balances user requests across multiple servers no single server overloaded.
